# @hwfa/ui — shared design system (Phase 1)

**Not yet implemented.** Cross-platform components shared by `apps/mobile` and
`apps/web`, including the inline scam-warning banner (spec §Scam Warning — Inline
UI) which renders above a message bubble using the `ScamVerdict` type from
`@hwfa/models`.
