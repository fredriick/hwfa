# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo
.turbo/

# Go
backend/**/bin/
*.exe
*.exe~
*.test
*.out

# Discovery local persistence (dev only; contains salted hashes, no raw numbers)
backend/discovery/discovery-*-data.json
backend/discovery/discovery-*-data.json.tmp

# Relay local persistence (dev only; opaque ciphertext envelopes)
backend/relay/relay-*-queue.json
backend/relay/relay-*-queue.json.tmp

# Secrets & keys — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.keystore
# Firebase / service-account configs (embed API keys / private keys)
google-services.json
**/google-services.json
*service-account*.json
*.p8
*.p12
secrets/

# Local databases
*.sqlite
*.sqlite3
*.db

# ML models (delivered via CDN, not repo)
*.onnx
*.pt
*.pth

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Test / coverage
coverage/
.nyc_output/
